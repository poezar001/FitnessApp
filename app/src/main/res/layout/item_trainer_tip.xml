<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:cardCornerRadius="16dp"
    app:cardBackgroundColor="@color/surface"
    android:layout_margin="4dp"
    android:elevation="2dp">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Full-width Image Banner (Background) -->
        <ImageView
            android:id="@+id/ivTrainerImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/trainer_dice" />

        <!-- Dark Overlay for Text Readability -->
        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#88000000" />

        <!-- Content on Top of Image -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="bottom"
            android:padding="16dp">

            <!-- Trainer Name -->
            <TextView
                android:id="@+id/tvTrainerName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Kim Ngo"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF" />

            <!-- Trainer Role -->
            <TextView
                android:id="@+id/tvTrainerRole"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="Pilates Instructor"
                android:textSize="14sp"
                android:textColor="#CCFFFFFF" />

            <!-- Tip Text -->
            <TextView
                android:id="@+id/tvTipTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="Core strength is the foundation of all movement"
                android:textSize="16sp"
                android:textColor="#FFFFFF"
                android:lineSpacingExtra="3dp" />

        </LinearLayout>

    </FrameLayout>
</androidx.cardview.widget.CardView>